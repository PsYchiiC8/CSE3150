;problem 1

(define (fact n)
  (let ((total 1)
        (count 1))
    (define (help)
      (cond ((= count n) 'done)
            (else
             (set! count (+ count 1))
             (set! total (* total count))
             (help))))
    (help)
    total)
)